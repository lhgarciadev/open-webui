# Etapa 5.1 - Eliminar Transparencias y Mejorar Responsiveness

## Contexto

Multiples componentes usan transparencias (`/50`, `/60`, `/80`) que causan bleed-through de texto. Ademas, algunos contenedores tienen anchos fijos sin breakpoints responsivos.

## Archivos a Modificar

```
src/lib/components/chat/MessageInput.svelte
src/lib/components/chat/MessageInput/VoiceRecording.svelte
src/lib/components/chat/MessageInput/FilesOverlay.svelte
src/lib/components/chat/MessageInput/InputMenu.svelte
src/lib/components/chat/MessageInput/IntegrationsMenu.svelte
src/lib/components/chat/Messages/Message.svelte
src/lib/components/layout/Sidebar.svelte
```

## Instrucciones

### 1. MessageInput.svelte - Queue Container

**Buscar (linea ~1151):**
```svelte
class="mb-1 mx-2 py-0.5 px-1.5 rounded-2xl bg-gray-900/60 border border-gray-800/50 overflow-hidden"
```

**Reemplazar con:**
```svelte
class="mb-1 mx-2 py-0.5 px-1.5 rounded-2xl bg-gray-100 dark:bg-gray-900 border border-gray-200 dark:border-gray-800 overflow-hidden"
```

### 2. Sidebar.svelte - Hover Buttons

**Buscar todas las instancias de:**
```
hover:bg-gray-100/50 dark:hover:bg-gray-850/50
```

**Reemplazar con:**
```
hover:bg-gray-100 dark:hover:bg-gray-850
```

Hay aproximadamente 2-3 instancias en lineas ~906 y ~932.

### 3. VoiceRecording.svelte

**Buscar (linea ~396):**
```
bg-gray-100/50 dark:bg-gray-850/50
```

**Reemplazar con:**
```
bg-gray-100 dark:bg-gray-850
```

**Buscar (linea ~405):**
```
bg-gray-200 dark:bg-gray-700/50
```

**Reemplazar con:**
```
bg-gray-200 dark:bg-gray-700
```

### 4. FilesOverlay.svelte

**Buscar (linea ~28):**
```
bg-gray-100/50 dark:bg-gray-900/80
```

**Reemplazar con:**
```
bg-gray-100 dark:bg-gray-900
```

### 5. InputMenu.svelte - Hover States

**Buscar y reemplazar TODAS las instancias de:**
```
dark:hover:bg-gray-800/50
```

**Reemplazar con:**
```
dark:hover:bg-gray-800
```

Hay aproximadamente 10+ instancias en este archivo.

### 6. IntegrationsMenu.svelte - Hover States

**Buscar y reemplazar TODAS las instancias de:**
```
dark:hover:bg-gray-800/50
```

**Reemplazar con:**
```
dark:hover:bg-gray-800
```

Hay aproximadamente 8+ instancias en este archivo.

### 7. Message.svelte - Responsiveness

**Buscar:**
```svelte
class="... max-w-5xl ..."
```

**Reemplazar con (agregar breakpoints):**
```svelte
class="... max-w-full sm:max-w-xl md:max-w-2xl lg:max-w-4xl xl:max-w-5xl ..."
```

## Resumen de Patrones

| Buscar | Reemplazar |
|--------|------------|
| `bg-gray-100/50` | `bg-gray-100` |
| `dark:bg-gray-850/50` | `dark:bg-gray-850` |
| `dark:bg-gray-900/80` | `dark:bg-gray-900` |
| `dark:bg-gray-700/50` | `dark:bg-gray-700` |
| `dark:hover:bg-gray-800/50` | `dark:hover:bg-gray-800` |
| `hover:bg-gray-100/50` | `hover:bg-gray-100` |
| `dark:hover:bg-gray-850/50` | `dark:hover:bg-gray-850` |
| `bg-gray-900/60` | `bg-gray-100 dark:bg-gray-900` |
| `border-gray-800/50` | `border-gray-200 dark:border-gray-800` |

## Tip: Busqueda Global

Usar busqueda global en el editor (Cmd+Shift+F / Ctrl+Shift+F):
- Buscar: `/50`
- Filtrar por: `src/lib/components/chat/`

## Rollback

```bash
git checkout HEAD -- src/lib/components/chat/
git checkout HEAD -- src/lib/components/layout/Sidebar.svelte
```

## Siguiente Paso

Ejecutar `5.2-transparency-responsive-validar.md` para validacion final.
